'use strict';

describe('Service: mySpecialService', function() {

  var x;

  beforeEach(function() {
    x = null;
  });

  it('should do something meaningful', function() {
    expect(x).toBeDefined();
  });

  it('should do something meaningful', function() {
    expect(x).toBeNull();
  });

});